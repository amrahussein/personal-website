/**
 * priorities:
 * An object containing three keys representing different priority levels:
 *  NORMAL, HIGH, IMPORTANT - used for sorting
 *
 * @typedef {Object} Priority
 * @property {string} NORMAL - normal priority
 * @property {string} HIGH - high priority
 * @property {string} IMPORTANT - important priority
 */

export const priorities = Object.freeze({
  NORMAL: 0,
  HIGH: 1,
  IMPORTANT: 2,
});
